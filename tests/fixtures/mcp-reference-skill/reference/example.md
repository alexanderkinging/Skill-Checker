# MCP Server Configuration Examples

Multiple mcp-server references in documentation:

```json
{ "mcp-server-a": { "command": "node" } }
```

```json
{ "mcp-server-b": { "command": "python" } }
```

```json
{ "mcp-server-c": { "command": "deno" } }
```
